#include<iostream>
using namespace std;
int main(){
    int x;
    cin>>x;
    // if(x%==2) cout<<"even";
    // else cout<<"odd";
    //it is the substiitue of if  else
//ternary operator right to left solve hota hai
    //condition ? true : false
    (x%2==0)  ? cout<<"even" : cout<<"odd" ;

}